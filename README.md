<p align="center">
  <img src="./README_Assets/app_icon.png" alt="GameHub Icon" width="120" />
</p>

# GameHub

GameHub — учебное iOS-приложение для поиска и просмотра игр на базе RAWG API.  
Можно смотреть трендовые и популярные игры, открывать подробности и добавлять их в избранное (с сохранением в Core Data).

Проект сделан как итоговая работа, где собраны вместе:
VIPER, координатор, async/await, свой сетевой слой, кастомный UI, Core Data и т.д.

---

## 🚀 Возможности

- 📊 **Главный экран (Home)**
  - Баннер с выделенной игрой
  - Горизонтальные списки:
    - Популярные игры
    - Топ по рейтингу
    - Скоро выйдут
  - Загрузка данных с RAWG API

- 🕹 **Экран деталей игры**
  - Постер игры
  - Название, жанр, рейтинг
  - Кнопка «избранное» в `UINavigationBar`
  - Состояние избранного синхронизировано с Core Data

- ⭐️ **Экран избранного (Favorites)**
  - Список сохранённых игр
  - Кастомные ячейки с мини-картинками
  - Свайп-удаление (Delete)
  - Пустое состояние при отсутствии избранного

- 🎨 **UI / UX**
  - Кастомный градиентный фон (отдельный view)
  - Поддержка **светлой и тёмной темы**
  - Единство стилей через цветовые токены (`.textPrimary`, `.cardBackground` и т.п.)

- 💾 **Хранение данных**
  - Core Data для избранных игр
  - Уникальный ключ `id` у `FavoriteGame`

---

## 🧱 Технологии

- **Язык:** Swift
- **UI:** UIKit, SnapKit, UICollectionView Compositional Layout, UITableView
- **Архитектура:** VIPER + Coordinator
- **Сетевой слой:** URLSession + async/await, собственный `NetworkService`
- **API:** [RAWG.io](https://rawg.io/apidocs)
- **Хранение:** Core Data (`FavoriteGame` + `FavoriteGameStorage`)
- **Кэширование картинок:** `NSCache` + свой `ImageLoader`
- **Темизация:** dynamic colors + `Colors.xcassets`

---

## 🏛 Архитектура

Проект организован по VIPER:

- **V (View / ViewController)**
  - Отвечает за отображение данных и UI
  - Делегирует события презентеру через протокол `*ViewOutput`

- **I (Interactor)**
  - Бизнес-логика модуля
  - Получает данные из сервисов (`GameService`, `FavoriteGameStorage` и др.)
  - Отдаёт результат презентеру через `*InteractorOutput`

- **P (Presenter)**
  - Связывает View и Interactor
  - Готовит UI-модели (`ViewModel`)
  - Вызывает роутер при навигации

- **E (Entity)**
  - Доменные модели: `Game`, `HomeSection`, `FavoriteGameViewModel` и т.п.

- **R (Router)**
  - Навигация внутри модуля
  - Вызовы координатора

Поверх этого — **Application Coordinator**:

- `AppCoordinator` создаёт таббар с двумя навконтроллерами:
  - Home
  - Favorites
- Навигация реализована через протоколы:
  - `HomeNavigation`
  - `FavoritesNavigation`
  - `GameDetailsNavigation`

---

## 📂 Структура проекта
Упрощённо:

```text
Application/
 ├── AppCoordinator
 └── AppDependencies

Modules/
 ├── Home (VIPER)
 ├── Favorites (VIPER)
 └── GameDetails (VIPER)

Data/
 ├── Networking (NetworkService, Endpoints)
 └── Storage (CoreData, FavoriteGameStorage)

Common/
 ├── UI (GradientBackgroundView, colors)
 └── Utilities (ImageLoader, extensions)

Resources/
 ├── Assets
 └── Info.plist
```text

## 🛠 Установка и запуск

### 1. Клонировать проект

```sh
git clone https://github.com/USERNAME/GameHub.git
cd GameHub
```

---

## 📸 Скриншоты

| Home (Dark) | Home (Light) |
|-------------|--------------|
| <img src="README_Assets/home_dark.png" width="330"> | <img src="README_Assets/home_light.png" width="330"> |

| Game Details | Favorites |
|--------------|-----------|
| <img src="README_Assets/details.png" width="330"> | <img src="README_Assets/favorites.png" width="330"> |

---

## 🎥 Demo (GIF)

<p align="center">
  <img src="README_Assets/demo.gif" width="380">
</p>
